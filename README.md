# Logging-HTTP-Request
Implementing a Middleware-Based Request Logger

Tasks implemented:
Capturing incoming requests (method, URL, timestamp).
Logging requests to a file (logs.txt).
Allowing further processing by other middleware layers.
Applied to a simple API with at least one route.

Key Features:
Structured middleware system.
Easy to extend with additional middleware.
Lightweight and efficient.
